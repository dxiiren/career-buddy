# carreer-buddy-proto — Project Memory Index

One line per memory below (`- [Title](file.md) — hook`). Each memory is one fact in its own `*.md` file with
frontmatter (`name`, `description`, `metadata.type` = reference | feedback | project). This index is loaded
each session; the fact files are read on demand when relevant. See the `## Memory` section in `CLAUDE.md` for
the protocol. Single-developer project — no team/relationship memory.

(No memories yet — add the first as `- [Title](file.md) — hook` when a durable fact is worth keeping.)
